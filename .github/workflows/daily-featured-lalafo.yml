name: Retired automatic featured shortlist

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: daily-featured-lalafo
  cancel-in-progress: false

jobs:
  manual-only-notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic recommendations are disabled; use the manual Telegram bot"
